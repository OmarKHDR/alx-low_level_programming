#include "main.h"
/**
 * _islower - my func
 * @c: the arg
 * Description: This function uses _putchar to print to the
 * standard output
 * Return: int
*/
int _islower(int c)
{
	return (islower(c) ? 1 : 0);
}
